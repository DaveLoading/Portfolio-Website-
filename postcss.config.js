// postcss.config.js defines CSS processors for build/dev.
export default {
  plugins: {
    // Tailwind builds utility classes.
    tailwindcss: {},
    // Autoprefixer adds browser prefixes so styles work in more places.
    autoprefixer: {},
  },
};
